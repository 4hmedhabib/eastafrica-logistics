import { Typography } from "@mui/material";
import { FC } from "react";

const TradeTicket: FC = (): JSX.Element => {
  return (
    <div>
      <Typography variant="h2" component="div">
        Trade Ticket
      </Typography>
    </div>
  );
};

export default TradeTicket;
